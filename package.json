{
  "name": "monorepo-template",
  "description": "Reusable React, Vite, and Turborepo project template",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "eslint . && turbo run lint",
    "knip": "knip",
    "check-types": "tsc -p tsconfig.tools.json && turbo run check-types",
    "pretest": "node packages/vitest-config/scripts/clean-blob-reports.ts",
    "test": "turbo run test",
    "test:projects": "vitest run",
    "test:projects:watch": "vitest --watch",
    "report": "pnpm test && pnpm --filter @repo/vitest-config report",
    "prepare": "husky",
    "lint-staged": "lint-staged",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "pnpm lint && pnpm knip && pnpm format:check && pnpm check-types && pnpm build && pnpm report"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.3",
    "@commitlint/config-conventional": "^21.2.3",
    "@commitlint/types": "^21.2.3",
    "@repo/eslint-config": "workspace:*",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "eslint": "catalog:",
    "husky": "^9.1.7",
    "jsdom": "catalog:",
    "knip": "^6.37.0",
    "lint-staged": "^17.5.1",
    "prettier": "^3.9.6",
    "turbo": "^2.10.12",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=24.20.0"
  },
  "packageManager": "pnpm@12.1.0"
}
